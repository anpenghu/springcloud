#配置端口
server:
  port: 80

#配置spring
spring:
  application:
    name: cloud-consumer-order
  cloud:
    zookeeper:
      #配置zookeeper的服务地址,将该客户端注册到zookeeper服务端中
      connect-string: 192.168.43.210:2181

